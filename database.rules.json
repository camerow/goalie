{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "action-items": {
      "$item": {
        ".write": "data.child('uid') === auth.uid",
        ".read": true
      }
    }
  }
}
